# 需求对标复核报告

> 复核人：rvw-rigel（沈墨·Rigel）
> 复核日期：2026-09-03
> 项目路径：`legal-management-system`
> 复核范围：需求文档核心模块 vs 实际代码（后端 61+ API、前端 10 页面、13 Prisma 模型 + 11 enum）

---

## 一、结论摘要

| 指标 | 结果 |
|------|------|
| 需求模块总数 | 11 |
| ✅ 完成 | 7（客户、业务事项、通讯中枢、计时收费、任务、用户管理、文档管理、Dashboard —— 按"核心可交付"口径为 7 项完整 + 2 项部分） |
| ⚠️ 部分完成 | 3（用户管理与权限、发票管理、任务管理-看板/分配） |
| ❌ 未实现 | 2（虚拟法务部、OA/ERP 对接） |
| 需求未规划但已开发 | 4 类（TimelineEvent 时间线、SystemLog 模型、EnterpriseConfig 模型、Redis 缓存层） |
| 技术方向偏离 | 1 项（SQLite→MySQL）；Electron 已实施（非偏离） |

**总体判断**：项目已完成"个人法务工作室"核心业务闭环（客户→业务→沟通→计时→发票→文档→看板），是一套**可运行的单人法务业务管理系统**。但需求中"虚拟法务部""OA/ERP 对接"两大差异化卖点**仅停留在数据模型预留层，无任何 API/前端实现**，属于需求与实现的实质性差距。RBAC 的角色管理（增删改）、发票付款明细、任务分配、看板完整形态为部分完成。

---

## 二、逐模块对标明细

### 1. 客户管理 — ✅ 完成

**需求**：个人/企业客户 CRUD，服务计划分类，客户关联业务

**实现**：
- `Client` 模型完整区分个人/企业（PERSONAL/ENTERPRISE），个人字段（gender/idNumber/birthDate）与企业字段（creditCode/legalRep/industry/scale/website）齐全
- CRUD + 统计 + `getClientMatters`（客户关联业务列表）后端接口齐全（7 个端点）
- 服务计划字段存在：`servicePlan`/`monthlyFee`/`serviceStart`/`serviceEnd`，`getStats` 含 `byServicePlan` 统计
- 前端 `ClientListPage` 支持列表/搜索/类型筛选/创建/编辑/详情抽屉/删除

**差距**：
- ⚠️ 服务计划分类：后端字段与统计完整，但**前端表单未暴露 `servicePlan` 等字段**（表单仅含基础联系信息），"服务计划分类"在前端不可录入/查看，属部分完成（后端 100% / 前端 0%）
- 标签 `tags` 筛选：后端 `findAll` 中 `tags` 查询被注释为"暂未实现"

---

### 2. 业务事项跟踪 — ✅ 完成（类型为超集）

**需求**：6 种事项类型（诉讼/非诉/顾问/合同/合规/其他），状态流转，截止日期

**实现**：
- `Matter` 模型 + 9 个端点：CRUD、`findByNo`、`stats`、`getTimeline`、`updateStatus`
- 状态流转 `updateStatus` 支持自动写 `completedAt`（COMPLETED）与 `startDate`（IN_PROGRESS），并写入 `TimelineEvent` 审计
- 业务编号自动生成（前缀+日期+序号，如 `SS-20260903-001`）
- 截止日期 `deadline` 字段 + `stats` 中"7 天内到期"查询

**类型对比**（需求 6 类 vs 实际 10 类）：
| 需求类别 | 实际 MatterType | 结论 |
|---------|----------------|------|
| 诉讼 | CASE_LITIGATION / CASE_ARBITRATION / CASE_MEDIATION | 细化拆分（超集） |
| 非诉 | CONSULTATION / TRAINING / DOCUMENT_DRAFT | 细化（超集） |
| 顾问 | COMPLIANCE | 对应 |
| 合同 | CONTRACT_REVIEW / CONTRACT_DRAFT | 细化（超集） |
| 合规 | COMPLIANCE | 对应 |
| 其他 | OTHER | 对应 |

> 结论：实际类型是需求的**细化超集**，需求 6 大类全部覆盖。✅

---

### 3. 通讯中枢 — ✅ 完成（渠道为超集）

**需求**：多渠道沟通记录（电话/邮件/微信/面谈/其他），双向标记，关联客户/业务

**实现**：
- `Communication` 模型 + 6 端点（CRUD + stats），支持按 `channel`/`direction`/`clientId`/`matterId` 筛选
- 双向标记：`Direction` enum（INBOUND/OUTBOUND）✅
- 关联客户 + 业务（clientId + matterId）✅
- `stats` 含按渠道/方向分组统计
- 前端 `CommunicationPage` 完整（列表/搜索/渠道筛选/创建/编辑/详情抽屉）

**渠道对比**（需求 5 类 vs 实际 8 类）：
需求：电话/邮件/微信/面谈/其他 → 实际：WECHAT/EMAIL/SMS/PHONE/MEETING/VIDEO/SYSTEM/OTHER
> 新增短信、视频、系统消息 3 种，需求 5 类全部覆盖 ✅

---

### 4. 虚拟法务部 — ❌ 未实现（仅数据模型预留）

**需求**：部署虚拟法务部，对接公司 OA，AI+真人协同配置服务团队

**实现现状**：
- 仅有 `EnterpriseConfig` 模型（服务等级 serviceLevel、响应时间 responseTime、月度配额 monthlyQuota/usedQuota、OA 对接字段 oaWebhookUrl/oaApiKey/oaApiSecret、钉钉/企业微信 token、成员列表 members、品牌定制 portal*）
- `clientService.getById` 中 `include: { enterpriseConfig: true }` 仅作详情附带查询
- **无任何** EnterpriseConfig 的 controller / service / routes（全项目 grep 确认无活跃端点）
- **无任何**前端"虚拟法务部/企业门户/服务团队"页面
- seed.ts 中有 2 条 `enterpriseConfig.create` 示例数据

**结论**：❌ 需求核心功能完全未实现，仅落库预留字段。AI+真人协同、OA 对接、服务团队配置均为零。

---

### 5. 预留 OA/ERP 对接 — ❌ 未实现（预留字段）

**需求**：预留接口对接外部财务/HR 系统

**实现现状**：
- `EnterpriseConfig` 含 `oaWebhookUrl`/`oaApiKey`/`oaApiSecret`/`dingtalkToken`/`wecomCorpId` 等**预留字段**（纯数据列，无写入/调用逻辑）
- 无任何对接 API、webhook 接收端点、出站调用代码

**结论**：⚠️/❌ 仅"预留了数据字段"，未实现任何"接口对接"。按需求"预留接口"字面，字段预留可视为最低限度满足；但无接口层，实质为未实现。

---

### 6. 计时收费 — ✅ 完成（发票关联前端缺失）

**需求**：开始/停止计时，费率，可计费标记，发票关联

**实现**：
- `TimeEntry` 模型 + 9 端点：CRUD、stats、`start`、`stop`、`createManual`
- `start` 带"同一用户不可并发计时"守卫；`stop` 用原子 where 守卫防并发重复停止
- 费率：`hourlyRate`（自动继承 Matter.hourlyRate）+ `amount` 计算
- 可计费标记：`isBillable`；开票标记：`isBilled`
- 前端 `TimeEntryPage` 支持开始/停止（实时秒表）、记录列表、统计卡片
- 发票关联：后端 `linkTimeEntries` 端点 + `invoiceStore.linkTimeEntries` 方法完整

**差距**：
- ⚠️ 前端 `InvoiceListPage` **无"关联计时记录"入口**（`linkTimeEntries` 有 store 方法但页面未调用），发票关联计时在 UI 层不可操作，属部分完成

---

### 7. 任务管理 — ⚠️ 部分完成

**需求**：看板模式，优先级，截止日期，分配

**实现**：
- `Task` 模型 + 7 端点：CRUD、stats、`toggleStatus`
- 优先级 `Priority`（LOW/MEDIUM/HIGH/URGENT）✅
- 截止日期 `dueDate` + 逾期查询 ✅
- 分配字段：模型有 `userId`（负责人，必填）✅
- 前端 `TaskBoardPage`

**差距**：
- ⚠️ **看板模式不完整**：当前为"待办/进行中"+"已完成"两列，非标准多列看板（无"进行中"独立列、无拖拽）
- ⚠️ **分配不可用**：`taskService.create` 硬编码 `userId = 当前登录用户`，前端创建表单**无"负责人"选择框**，无法将任务分配给他人
- `reminderAt`/`isReminded` 提醒字段模型存在，但无提醒调度逻辑

---

### 8. 用户管理与权限 — ⚠️ 部分完成

**需求**：RBAC，用户 CRUD，角色权限

**实现**：
- `User`/`Role` 模型 + 用户 7 端点：列表/详情/创建/更新/删除（软删）/重置密码/`getRoles`
- RBAC 中间件 `checkPermission`：支持通配 `*`、数组 `module:action`、对象 `{module:[actions]}` 三种权限格式，动作归一化（view→read 等）
- 角色种子 4 个：ADMIN(`*`)/MANAGER/LAWYER/ASSISTANT
- JWT 认证 + 登录限流（15 分钟 10 次）+ bcrypt 哈希
- 前端 `UserListPage` 完整（列表/搜索/创建/编辑/停用/重置密码/角色选择）

**差距**：
- ❌ **角色管理（增删改）缺失**：仅 `getRoles` 只读，无 createRole/updateRole/deleteRole 端点，前端也无角色管理页；角色只能读、不可维护
- 授权覆盖不均：tasks/communications/time-entries/invoices/documents/matters/clients 路由**仅 `authenticateToken`，未挂 `checkPermission`**；仅 users 路由挂了 `checkPermission`

---

### 9. 发票管理 — ⚠️ 部分完成

**需求**：发票编号自动生成，金额计算，付款记录，状态流转

**实现**：
- `Invoice` 模型 + 8 端点：CRUD、stats、`linkTimeEntries`、`recordPayment`
- 发票编号自动生成 `INV-YYYYMMDD-XXXX` ✅
- 金额计算：subtotal + taxRate→taxAmount − discount = totalAmount（含非负/上限保护）✅
- 状态流转：DRAFT/ISSUED/SENT/PARTIAL/PAID/OVERDUE/CANCELLED；`recordPayment` 自动流转 PARTIAL/PAID ✅
- 前端 `InvoiceListPage` 完整（统计卡片/创建/编辑/付款/删除）

**差距**：
- ⚠️ **付款记录缺失明细**：仅 `paidAmount` 累计 + 单一 `paidAt` 时间戳，**无独立 Payment 实体/多笔付款历史明细**；"付款记录"需求未完整满足
- OVERDUE 状态无自动判定逻辑（依赖人工设置）

---

### 10. 文档管理 — ✅ 完成

**需求**：文档元数据管理，分类，关联客户/业务

**实现**：
- `Document` 模型 + 6 端点（CRUD + stats），分类 `category`（CONTRACT/EVIDENCE/DELIVERABLE/TEMPLATE/OTHER），关联 clientId/matterId ✅
- 路径遍历防护（`filePath` 含 `..`/`//` 校验）
- `stats` 按分类分组 + 总大小统计
- 上传中间件 `upload.ts` 完整（文档/图片/头像/通用/知识库 5 种 multer 配置 + 类型过滤 + 大小限制），但**文档 CRUD 端点未接入 multer**（`documentRoutes` 无 upload 中间件，仅元数据 CRUD，`create` 手动传 `fileName`/`filePath`）
- 前端 `DocumentListPage` 完整（统计卡片/分类筛选/搜索/创建/编辑/删除）

**差距**：
- ⚠️ 实际文件上传（multer）与文档元数据 CRUD **脱节**：上传中间件已写好但未挂载到文档路由，前端"新建文档"是手动填 `filePath`（placeholder `/uploads/xxx.pdf`），无真实文件上传按钮

---

### 11. Dashboard — ✅ 完成

**需求**：聚合统计，图表可视化

**实现**：
- `dashboardService.getDashboardSummary` 一次性聚合客户/业务/任务/计时/发票/文档六维统计（Promise.all 并发）
- 前端 `DashboardPage` 使用 Recharts：饼图（业务状态分布）、柱状图（任务状态、发票金额）、收款率进度条、逾期任务/近期业务/7 天到期业务列表、6 个可点击统计卡片
- 计时时长/金额/文档大小格式化

> 需求完全满足 ✅（图表可视化 + 聚合统计均落地）

---

## 三、技术方向偏离评估

| 规划 | 实际 | 偏离程度 | 说明 |
|------|------|---------|------|
| 桌面端优先（Electron） | ✅ 已实施 | 无偏离 | `electron 42.3.0` + `electron-builder 26.8` 齐备；`frontend/electron/main.js`（主进程：spawn 后端、系统托盘、菜单、窗口）+ `preload.js` 完整；`package.json` 含 `dev:electron`/`build:win/mac/linux`/`start` 脚本与完整 `build` 配置（NSIS/DMG/AppImage、图标、extraResources 打包 backend） |
| 本地部署优先 | ⚠️ 部分 | 轻微偏离 | Electron 将后端 `dist/index.js` 作为 extraResources 本地启动（应用本地化 ✅），但后端仍**强依赖外部 MySQL 服务**（见下），非纯单机开箱即用 |
| SQLite 替代 MySQL | ❌ 未落地，仍用 MySQL | **实质偏离** | `schema.prisma` 大量 `@db.VarChar`/`@db.Decimal`/`@db.DateTime(0)`/`@db.Date` MySQL 专属注解；`.env` `DATABASE_URL="mysql://root:...:3306/legal_management"`。SQLite 方案完全未实施 |
| （未规划）Redis | 额外引入 | 超出需求 | `index.ts` 初始化 Redis 客户端（ioredis），但失败自动降级为内存，README 亦声明 Redis 依赖。需求未提缓存层 |

**偏离结论**：桌面端（Electron）符合需求且已落地；**SQLite→MySQL 是唯一明确的技术偏离**——需求规划用 SQLite 以利本地轻量部署，实际沿用 MySQL，导致"本地部署"仍需用户自行安装/连接 MySQL 数据库，削弱了"个人法务工具开箱即用"的定位。

---

## 四、需求未实现 / 部分实现清单（汇总）

### ❌ 完全未实现
1. **虚拟法务部**（模块 4）—— 无 API、无前端、无 AI+真人协同，仅 EnterpriseConfig 数据模型预留
2. **OA/ERP 对接**（模块 5）—— 仅 EnterpriseConfig 预留字段，无接口/无对接逻辑
3. **角色管理（增删改）**—— 仅 getRoles 只读

### ⚠️ 部分实现
4. 发票**付款记录明细** —— 无独立 Payment 实体，仅累计 paidAmount
5. 任务**分配** —— 模型有 userId，但创建硬编码为本人、前端无负责人选择
6. 任务**看板模式** —— 仅两列，非完整看板
7. 发票**关联计时前端入口** —— 后端/store 有，页面无 UI
8. 客户**服务计划分类前端** —— 后端字段+统计有，前端表单未暴露
9. 文档**真实文件上传** —— multer 中间件已写但未挂载，前端手动填路径
10. 权限**授权覆盖不均** —— 仅 users 路由挂 checkPermission，其余模块仅认证不鉴权

---

## 五、需求未规划但已开发（超出范围）

1. **TimelineEvent 时间线** —— 业务进展时间线模型 + `getTimeline` 端点 + 状态流转/信息更新自动写审计（需求 11 项未提，属增值）
2. **EnterpriseConfig 企业配置模型** —— 服务等级/配额/OA 预留/成员/品牌定制（为虚拟法务部预留，但功能未做）
3. **SystemLog 系统日志模型** —— 完整字段（action/resource/ip/userAgent/status），但**无写入端点，模型空置**
4. **Redis 缓存层** —— ioredis 引入 + 降级逻辑（需求未提）
5. **业务类型/沟通渠道超集** —— 事项 10 类（需求 6 类）、沟通 8 渠道（需求 5 类）
6. **附加字段** —— 任务提醒 `reminderAt`/`isReminded`、业务进度 `progress`/`nextAction`、客户标签/来源 `tags`/`source`

---

## 六、其他发现（非需求项，但影响质量）

1. **API 口径差异**：任务描述"61 个接口"，实测路由端点约 66 个（含 `auth/register`、`auth/refresh` 各计 1；users 实际 7 个端点含 `/roles`）。差异源于口径（register 未计入、users 数量出入），功能无损。
2. **前端路由缺口**：`AppLayout` 用户菜单含 `profile` → `navigate('/profile')`，但 `App.tsx` **无 `/profile` 路由**，点击会落入 `*` 兜底跳回首页。
3. **遗留 `.disabled` 文件**：`backend/src/controllers` 与 `routes` 下存在 8 个 `.ts.disabled` 文件（agentController/contractController/dashboardController(旧)/knowledgeBaseController 及其 routes），为早期 SaaS 版（合同/AI Agent/知识库）遗留，已从路由卸载，但**对应 Prisma 模型（Agent/KnowledgeBase 等）未出现在当前 schema**（schema 仅 12 模型，无 Agent/KnowledgeBase），说明早期模型已清理，代码残留未删。
4. **README 与现状脱节**：README 仍描述"合同管理/案件管理/AI Agent 管理/服务产品与订单/在线接单"等早期 SaaS 定位与路线图，与实际"个人法务工作室"定位及已交付模块不符（文档滞后）。
5. **编码隐患**：`InvoiceListPage.tsx` 存在多处中文乱码字符（如"已开�?/状�?/发票�?/开具日�?/金�?"），疑为写入时编码损坏，需修复。
6. **并发计时守卫为单用户维度**：`timeEntry.start` 仅按 `userId` 查未停止计时，非全局互斥，符合单人法务场景预期。

---

## 七、复核结论与建议

### 核心结论
项目**已交付"个人法务工作室"的核心业务闭环**，客户→业务→沟通→计时→发票→文档→任务→仪表盘全链路可用，且 Electron 桌面端已就绪、安全基线（JWT/bcrypt/限流/路径遍历防护/事务）扎实。**需求 11 项中 7 项完整交付、3 项部分交付、2 项（虚拟法务部、OA/ERP 对接）完全未实现**，另有 1 项技术偏离（SQLite→MySQL）。

### 优先建议（按影响排序）
1. **补齐 RBAC 角色管理与鉴权覆盖**：增加角色 CRUD，并为 clients/matters/tasks/invoices 等路由挂载 `checkPermission`，否则"权限"仅对用户管理生效，与"一人法务"中"协作者/助理"场景不符。
2. **决定虚拟法务部/OA 对接的去留**：当前仅数据模型，属"画饼"。若短期不做，建议在需求文档明确标记为"预留/二期"，避免对标误判。
3. **接通文档真实上传**：将已写好的 `uploadDocument` 中间件挂到 `documentRoutes`，前端加真实文件选择，替换手动填路径。
4. **补齐发票付款明细**：新增 Payment 实体记录每笔付款，支持部分付款历史。
5. **修复 UI 乱码与 profile 路由缺口**。
6. **明确数据库决策**：若坚持"本地部署优先"，评估迁回 SQLite 或提供嵌入式 DB（如 better-sqlite3）打包方案，消除对 MySQL 的强依赖。

---

*本报告基于对 `backend/src`、`frontend/src`、`backend/prisma/schema.prisma`、`frontend/electron` 的逐文件静态复核生成，未做运行时验证。*
