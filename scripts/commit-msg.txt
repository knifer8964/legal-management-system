M8: 发票管理后端API完成（invoiceService/Controller/Routes + 冒烟测试8/8通过）

新增文件:
- backend/src/services/invoiceService.ts — 发票CRUD+搜索+分页+统计+关联计时+支付记录
- backend/src/controllers/invoiceController.ts — 8个接口控制器
- backend/src/routes/invoiceRoutes.ts — 路由注册(stats在:id前)
- scripts/m8-smoke-test.js — M8冒烟测试脚本(8/8通过)

修改:
- backend/src/routes/index.ts — 注册 /invoices 路由

接口清单:
- GET /api/v1/invoices — 发票列表(分页+筛选+搜索)
- POST /api/v1/invoices — 创建发票(自动生成发票编号)
- GET /api/v1/invoices/:id — 发票详情
- PUT /api/v1/invoices/:id — 更新发票
- DELETE /api/v1/invoices/:id — 删除发票(解除计时关联)
- GET /api/v1/invoices/stats — 发票统计
- POST /api/v1/invoices/:id/link-time-entries — 关联计时记录
- POST /api/v1/invoices/:id/payment — 记录支付

虚拟团队首次并发协作: dev-cypher完成开发, tst-verity准备测试脚本
