Code Review 整改: 修复 rvw-rigel 发现的 4 个严重问题

🔴 #1 userStore 改为 hook 形式
   - export const useUserStore = create<UserStore>(...)
   - UserListPage 改用 useUserStore() 调用
   - 修复错误提取: e.response?.data?.error?.message

🔴 #2 删除 userService.ts 死代码
   - 全项目无引用，userController 已完整实现
   - 消除 Prisma 双轨架构漂移风险

🔴 #3 JWT 硬编码 fallback 已在上一个 commit 移除

🔴 #4 userController 错误处理改为 next(err)
   - 由全局 errorHandler 做 Prisma 错误映射
   - resetPassword 增加密码长度校验

前端修复:
- InvoiceListPage: 移除未使用 Card 导入, 删除改 async+try/catch, 错误提取统一
- DocumentListPage: 移除未使用 r 参数
- AppLayout: user?.name → user?.realName || username
- 前端 tsc --noEmit 零错误

回归测试 26/26 通过
